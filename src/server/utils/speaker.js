// Disabling because we intend to have more exports in the future.
/* eslint-disable import/prefer-default-export */

export const getBestSpeakerName = speaker => speaker.normalizedName || speaker.extractedName
