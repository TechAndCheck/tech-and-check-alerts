{{> head }}
{{> boilerplateTop }}

<div class="claim-section">
  <h1 class="claim-section__header">📺 TV Claims 📺</h1>
  <ul class="claims">
  {{#each claims}}
    <li class="claim">
      <div class="claim__metadata">
        <a href="#" class="claim__permalink">
          <cite class="claim__speaker">Speaker Name</cite>
          <span class="claim__platform">(<b>Platform</b>)</span></a>:
      </div>
      <blockquote class="claim__content">{{ content }}</blockquote>
    </li>
  {{/each}}
  </ul>
</div>

{{> boilerplateBottom }}
{{> foot }}
